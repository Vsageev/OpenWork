{
  "id": "basic",
  "name": "Basic Agent",
  "description": "A simple agent with instructions",
  "parameters": [
    {
      "key": "workingDirectory",
      "label": "Working directory",
      "description": "Optional directory this agent should treat as its default workspace.",
      "placeholder": "/Users/name/project",
      "type": "directory",
      "required": false,
      "contentTemplate": "- The project you are working on is `{{value}}`.\n - Use other paths only if the task requires it; ask first when avoidable.",
      "emptyContentTemplate": "- Work in the current folder by default for commands and file operations.\n- Use other paths only if the task requires it; ask first when avoidable."
    }
  ],
  "files": [
    { "type": "file", "name": "CLAUDE.MD", "template": "CLAUDE.MD.hbs", "models": ["Claude"] },
    {
      "type": "file",
      "name": "AGENTS.md",
      "template": "CLAUDE.MD.hbs",
      "models": ["Codex", "Cursor", "OpenCode", "Qwen"]
    }
  ],
  "defaultSkills": [
    "session-start",
    "openwork-api",
    "request-clarification",
    "session-end-memory",
    "verification"
  ]
}
